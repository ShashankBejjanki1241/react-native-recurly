/**
 * Shared image assets (`assets/images/`). App / splash / tab art also lives there
 * (see `app.json`); import here when a screen needs them in JS.
 */
import avatar from "@/assets/images/avatar.png";
import splashPattern from "@/assets/images/splash-pattern.png";

export default { splashPattern, avatar };
